version: '3.1'

services: 
 
  db:
    image: postgres
    restart: always
    environment:
      POSTGRES_PASSWORD: example #This is a purposely bad password for local development
      POSTGRES_USER: postgres #This is the default
    ports:
      - 5432:5432

  adminer:
    image: adminer
    restart: always
    ports:
      - 8080:8080